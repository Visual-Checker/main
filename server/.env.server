# 서버 설정
FLASK_APP=app.py
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=your-secret-key-change-this

# 데이터베이스 설정
DB_HOST=192.168.0.41
DB_PORT=5432
DB_NAME=devserver
DB_USER=orugu
DB_PASSWORD=orugu#0916

# JWT 설정
JWT_SECRET_KEY=your-jwt-secret-key-change-this
JWT_ACCESS_TOKEN_EXPIRES=3600

# 서버 설정
HOST=0.0.0.0
PORT=5000

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Face Detection 설정
FACE_DETECTION_MODEL=yunet  # yunet, retinaface, mediapipe
FACE_DETECTION_BACKEND=gpu  # gpu, cpu
CONFIDENCE_THRESHOLD=0.7

# Celery 설정
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ZeroMQ 설정
ZMQ_PORT=5555

