# 데이터베이스 설정
DB_HOST=192.168.0.41
DB_PORT=5432
DB_NAME=devserver
DB_USER=orugu
DB_PASSWORD=orugu#0916

# 서버 DB 정보
SERVER_DB_HOST=localhost
SERVER_DB_PORT=5432
SERVER_DB_NAME=devserver
SERVER_DB_USER=orugu
SERVER_DB_PASSWORD=orugu#0916

# 서버 API 설정
API_BASE_URL=http://localhost:5000/api

# Face Detection 설정
FACE_DETECTION_MODEL=yunet  # yunet, retinaface, mtcnn, mediapipe
CONFIDENCE_THRESHOLD=0.70
USE_GPU=True
FACE_SIMILARITY_THRESHOLD=0.70

# Hand Gesture Detection 설정
GESTURE_CONFIDENCE_THRESHOLD=0.7
GESTURE_COOLDOWN=3.0
GESTURE_MODEL_PATH=models/gesture_recognizer.task

# Voice Recognition 설정
VOICE_SIMILARITY_THRESHOLD=0.7
VOICE_MODEL_PATH=models/spkrec-ecapa-voxceleb

# 카메라 설정
CAMERA_INDEX=0
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
